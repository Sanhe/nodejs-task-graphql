mutation UpdateUser($id: ID!, $firstName: String!, $lastName: String!, $email: String!) {
    updateUser (id: $id, variables: {firstName: $firstName, lastName: $lastName, email: $email}) {
        id,
        firstName,
        lastName,
        email
    }
}
