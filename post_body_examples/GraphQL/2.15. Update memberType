mutation UpdateMemberType($id: ID!, $discount: Int, $monthPostsLimit: Int) {
    updateMemberType (id: $id, data: {discount: $discount, monthPostsLimit: $monthPostsLimit}) {
        id,
        discount,
        monthPostsLimit
    }
}
